<?xml version="1.0" encoding="utf-8"?>
<!--
  AccessibilityService config. The only capability we actually want is
  `canPerformGestures` (for dispatching taps + swipes). We declare the
  minimum event subscription (typeWindowStateChanged) because the framework
  requires at least one event type to be set, but we ignore those events
  in code. We deliberately do NOT request canRetrieveWindowContent because
  we don't need to inspect on-screen UI. This service is purely input dispatch.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="false"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description"
    android:summary="@string/accessibility_service_summary" />
